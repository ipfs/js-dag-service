{
  "name": "@textile/ipfs-lite-files",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean",
    "clean": "rimraf ./dist",
    "build": "npm run build:node && npm run build:browser",
    "build:node": "tsc -p tsconfig.json",
    "build:browser": "NODE_ENV=production webpack --mode production --config ../../webpack.config.js"
  },
  "devDependencies": {
    "@types/interface-datastore": "^0.8.0",
    "interface-datastore": "^0.8.0",
    "@types/node": "^13.1.7",
    "@textile/ipfs-lite-host": "^0.0.1",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "@textile/ipfs-lite-core": "0.0.1",
    "ipfs-unixfs-exporter": "^0.41.0",
    "ipfs-unixfs-importer": "^0.44.0",
    "loglevel": "^1.6.6",
    "typescript": "^3.7.4"
  }
}
